// Kept as a thin re-export so imports from the flat `components/` path and
// the grouped `components/(modal)/` path both resolve to the same component.
export { VoteSuccessModal as VotingSuccessModal } from "./(modal)/VoteSuccessModal";
