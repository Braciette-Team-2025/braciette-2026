export type FaqItem = {
  id: number;
  title: string;
  desc: string;
};

export type TimelineItem = {
  id: number;
  title: string;
  date: string;
  desc: string;
};

export type VotingGuideItem = {
  id: number;
  desc: string;
};
